# 2026-04-23 개발 진행 사항

브랜치: `feat/meeting-flow-redesign`
작업자: JW + Claude

---

## 1. 커밋 반영분 — `6784fe5` feat: redesign meeting flow and add account/meeting management

회의 생성 → 안건 등록 → 사전 검토 → 본회의 → 보고서 단계를 한눈에 파악·관리할 수 있도록 전체 프로세스 흐름을 재구성.

### Meeting lifecycle
- `/meetings/new` 2-step 위저드 추가 (회의 정보 + 안건 accordion)
- `/meetings/[id]` 상세 페이지 추가 — 4단계 타임라인과 단계별 CTA
- DB에서 다음 회차 번호 자동 초기화, (year, round) 중복 감지 시 inline 에러 표시
- 회의 회차 cascade delete (안건 + CEO 보고서) 및 confirm/toast 처리

### Dashboard
- 홈 화면을 현재 회의 중심으로 재설계 — 진행률 바, 단계별 CTA, 최근 회차 strip, Archive
- `CurrentMeetingCard` 컴포넌트 신규

### Account & navigation
- 관리자(경영전략실장 이석진) + 4명의 집행위원(전순창·원희용·차효석·김병호) 시드
- 계정 관리: 모달 → `/admin/users` 페이지로 승격. 관리자·간사 / 집행위원 섹션 분리
- AppHeader: 대시보드 · 회의 관리 · 계정 관리(관리자 전용), 중복된 안건 등록 버튼 제거

### Archive & live meeting
- Archive: 공유 컬럼(Web Share/클립보드) 추가, 제목 클릭 시 안건 상세 모달
- Live meeting: 첨부 파일명 클릭 시 미리보기 모달 오픈
- 회차 에디터에 정기회의 / 수시회의 토글 추가

**커밋 범위**: 16 파일 변경, +1544 / -452

---

## 2. 커밋 이후 작업분 (uncommitted)

커밋된 흐름 재구성을 바탕으로, 실제 데이터 연결 및 상호작용 세부 다듬기를 진행.

### 2.1 UI 스토어 — roundId 라우팅
`app/lib/store/uiStore.ts`
- `pendingRoundId`, `pendingPreviewRoundId`, `pendingAgendaRoundId` 필드 추가
- `openAgendaDrawer`, `openPreviewModal`, `openMeetingStartModal`이 roundId를 인자로 받도록 확장
- 모든 모달이 목데이터가 아닌 "현재 회의 회차" 기반으로 동작하도록 정비

### 2.2 PreviewDrawer (사전 검토) — 실제 데이터 연동
`app/components/modals/preview/PreviewDrawer.tsx` (+306 / -163)
- `mockLiveAgendas` 제거 → `meetingStore.rounds`에서 실제 안건 조회
- "검토 완료" 버튼 추가 → 안건 `voteResult`를 `approved`로 업데이트 (`updateRound`)
- 패널 전환 시 activeIndex 초기화, 오버레이 스타일 통일

### 2.3 ReportModal — 완료 회의만 표시
`app/components/modals/report/ReportModal.tsx` (+87 / -75)
- 로컬스토리지 `getCompletedMeetings` + mock 병합 로직 제거
- `meetingStore` 단일 소스 기반, `duration !== '00:00:00'` 필터로 완료 회의만 노출
- 레이아웃: 우측 drawer → 중앙 정렬 모달 (max-w-6xl, h-92vh)

### 2.4 CurrentMeetingCard — 일정/장소 인라인 편집
`app/components/dashboard/CurrentMeetingCard.tsx` (+137 / -26)
- 날짜·시간·장소 inline edit 모드 추가 (`PencilSimple` / `Check` / `X`)
- `meetingStore.updateRound`로 즉시 반영
- CTA "사전 검토 열기" → "회의 안건 검토"로 라벨 변경
- 미검토 안건 수 카운트(`pendingCount`) 및 검토 완료 상태 계산 추가

### 2.5 meetings 목록 — 진행중/완료 섹션 분리
`app/app/meetings/page.tsx` (+130 / -85)
- `duration` 유무 기준으로 `activeRounds` / `completedRounds` 분리 렌더링
- 진행중/완료 뱃지 기준을 "pending 안건 수" → "duration 값"으로 변경 (본회의 종료 여부 기준)

### 2.6 Meeting Start & Agenda Upload 재작업
- `MeetingStartModal.tsx` (+224 / -85): roundId 기반 회차 해석, 참석자/체크리스트 스토어 연동
- `AgendaUploadDrawer.tsx` (+220 / -75): pendingAgendaRoundId 기반 대상 회차 지정
- `ClosingPanel.tsx`: 종료 처리 경로 정리

### 2.7 Auth 스토어 — 기본 관리자 계정 변경
`app/lib/store/authStore.ts`
- 기본 관리자: `seokjin@humuson.com / humuson1234 (이석진)` → `admin / 1234 (관리자)`
- `admin-001` 항목은 로그인 시 항상 INITIAL_ADMIN 값으로 강제 덮어쓰기 (기존 데이터 정합성 보정)

### 2.8 기타
- `app/package.json`: `html2canvas@^1.4.1` 의존성 추가 (보고서 이미지/PDF 출력용)
- `app/components/archive/AgendaTable.tsx` (+92 / -59): 공유/상세 모달 연결 정비
- `app/components/modals/live-meeting/LiveMeetingModal.tsx`: 패널 라우팅 미세 조정
- `app/components/dashboard/WorkflowSection.tsx`, `app/app/login/page.tsx`, `app/app/meetings/[id]/page.tsx` 등 다수 후속 다듬기

---

## 3. 테스트·검증 상태

- 로컬 dev server에서 수동 검증 (스크린샷 `screenshots/` 저장)
- 자동화 테스트는 추가되지 않음 — 회귀 방지 테스트 미비 (후속 과제)

## 3.5 계정 관리 · 비밀번호 · 메일 발송 (추가 구현)

### 3.5.1 관리자 전용 접근 강화
- `/admin/users` 페이지: `isAdmin()` 가드로 비관리자 차단 (이미 적용)
- `AppHeader`: 계정 관리 네비 링크는 관리자에게만 노출 (이미 적용)
- 설명 문구 "관리자(간사)" → "관리자"로 정리

### 3.5.2 비밀번호 설정 · 재설정
- `app/lib/store/authStore.ts`: `resetPassword(userId, newPassword)` 추가
  - 현재 로그인 중인 사용자 대상일 경우 `currentUser` 및 localStorage 동시 동기화
- `app/lib/utils/password.ts` (신규): `generateTempPassword(length=10)`
  - 대/소/숫자/기호 1자씩 포함 보장, 혼동 문자(0·O·1·l·I) 제외
- `/admin/users` 사용자 행에 🔑 버튼 추가 → 재설정 모달 노출
  - 수동 입력 / 자동 생성 / 클립보드 복사 / 메일 발송 체크박스
  - 관리자·집행위원 모두에게 제공

### 3.5.3 계정 정보 메일 발송
- `app/lib/services/mailer.ts` (신규): nodemailer 기반 공용 `sendMail()`
- `app/lib/actions/sendCredentials.ts` (신규): Server Action, HTML 템플릿 (XSS escape 적용)
  - `kind: 'new' | 'reset'`로 메일 문구 분기
- 신규 등록 폼에 "등록 후 ID·비밀번호 메일 발송" 체크박스 추가
- 비밀번호 재설정 모달에도 동일한 발송 체크박스
- SMTP 미설정 시 `{ok:false, reason:'smtp_not_configured'}` 반환 → UI에서 명확한 토스트 안내
- 의존성 추가: `nodemailer@^8.0.5`, `@types/nodemailer`

### 3.5.4 메일 서버(SMTP) 설정 메뉴
런타임에서 관리자가 직접 SMTP 정보를 등록/수정할 수 있도록 UI 추가.

- `app/lib/services/smtpConfig.ts` (신규): 서버측 `.runtime/smtp.json` 파일 기반 설정 저장/로드
  - 우선순위: **파일 설정 > `.env.local` 환경변수 > 미설정**
- `app/lib/actions/smtpSettings.ts` (신규): Server Actions
  - `getSmtpSettings` — 비밀번호는 반환하지 않고 `hasPassword` 플래그만 노출
  - `saveSmtpSettings` — 비밀번호 필드가 비면 기존 값 유지
  - `clearSmtpSettings`, `testSmtpConnection`(verify), `sendTestMail`
- `app/components/admin/SmtpSettings.tsx` (신규): `/admin/users` 상단 접이식 카드
  - 프리셋 버튼: Gmail / Naver / Daum / Worksmobile — host·port·secure 자동 입력
  - 필드: 호스트 · 포트 · SSL · 메일 계정(ID) · 비밀번호 · From
  - 액션: **저장** / **연결 테스트** / **테스트 발송** / **초기화**
  - 상태 배지: `저장됨` · `환경변수` · `미설정`
  - Gmail 앱 비밀번호 안내 링크, 평문 저장 경고 문구 포함
- `.env.local.example`에 `SMTP_*` 항목 추가
- `app/.gitignore`에 `/.runtime/*.json` 추가 (비밀번호 평문 저장 파일 보호)

### 3.5.5 파일 변경 요약 (3.5 범위)
- 신규: `lib/services/mailer.ts`, `lib/services/smtpConfig.ts`, `lib/actions/sendCredentials.ts`, `lib/actions/smtpSettings.ts`, `lib/utils/password.ts`, `components/admin/SmtpSettings.tsx`
- 수정: `app/admin/users/page.tsx` (재설계), `lib/store/authStore.ts` (+resetPassword), `.env.local.example`, `app/.gitignore`, `package.json` (+nodemailer)

---

## 4. 다음 작업 제안
- Preview/Report 모달의 모바일 레이아웃 검증
- `duration` 기반 완료 판정을 명시적 `status` 컬럼으로 모델링 전환 고려
- authStore 로컬스토리지 마이그레이션 경로 문서화 (기존 사용자 이메일 로그인 → admin 계정 변경 안내)
- SMTP 비밀번호 평문 저장 → 암호화 저장 또는 Secrets Manager 연동
- 사용자 본인의 비밀번호 변경(셀프 서비스) 화면 — 현재는 관리자만 재설정 가능
- 메일 발송 실패 로그/재시도 큐
